# Immune-infiltration analysis

Run `Rscript run_analysis.R` from this directory. The bundled expression and phenotype inputs preserve the relative paths used by the original workflow. Compact reference outputs are located in `06_immune_infiltration/`.

The public dataset accession and download location must be confirmed in `../metadata/EXTERNAL_DATA_MANIFEST.tsv` before release.
